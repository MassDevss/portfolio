import { ClearExtensionPipe } from './clear-extension.pipe';

describe('ClearExtensionPipe', () => {
  it('create an instance', () => {
    const pipe = new ClearExtensionPipe();
    expect(pipe).toBeTruthy();
  });
});
